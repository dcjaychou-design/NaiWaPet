# 参与贡献

感谢关注并参与改进奶蛙桌宠。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 Windows 11 x64 和 .NET 10 SDK 开发。
3. 运行 `./build.ps1`，确保编译、核心测试和素材校验全部通过。
4. 如果修改了动画生成逻辑，按照 `docs/BUILDING.md` 重新生成素材，并检查透明边缘和所有阶段的动作。
5. 提交说明应解释用户可见变化；Pull Request 应避免混入无关格式化。

## 素材贡献

提交新图片、视频或声音时，必须同时说明可核验来源、许可、作者或生成方式，并提供原始文件 SHA-256。公开可访问不等同于可自由使用；来源或授权不明确的素材不应提交。

## 代码风格

- C# 开启 Nullable、隐式 using 和 warnings-as-errors。
- UI 文字优先使用简体中文，代码标识符使用英文。
- 核心算法尽量放入 `NaiWaPet.Core` 并补充测试。
- 保持普通用户无需管理员权限、无需预装运行库。
- README、更新日志和下载说明面向软件使用者书写，应直接说明功能、限制和操作方式，避免使用对话式或开发过程式表述。
- 发布版本必须使用语义化版本；修改版本时应确保文件名与程序集、文件和信息版本一致。
